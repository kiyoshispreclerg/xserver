sources:
    git:
        url:    ${@SOLUTION::xlibre_git}/xf86-input-joystick.git
        ref:    ${ref}
        depth:  1
        fetch:
          - refs/heads/*:refs/remotes/origin/*
          - refs/tags/*:refs/tags/*

buildsystem: autotools

build-depends:
    - x11/xserver-sdk

ref: origin/master
